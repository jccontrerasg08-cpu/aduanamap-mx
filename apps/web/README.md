# apps/web — Frontend (Next.js App Router)

Stub de Fase 1: landing bilingüe + estado de API. Próximo: mapa MapLibre con fallback accesible
(lista de países), página de país/tratado/fracción, calculadora.

```bash
npm install
npm run dev     # http://localhost:3000  (requiere la API en :8000)
```

Consume el envelope `data / source_trace / warnings` (tipos en `packages/schemas/ts/envelope.ts`).
